{
  "name": "@chax-at/prisma-filter",
  "version": "2.0.0-alpha.2",
  "description": "The PrismaFilter package allows the frontend to request filtered, sorted, and paginated data via query parameters. The package automatically generates Prisma findOptions from these query parameters, which can then be directly used in the backend.",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "repository": "github:chax-at/prisma-filter",
  "scripts": {
    "prepare": "npm run build",
    "build": "tsc",
    "publish": "npm publish --access public"
  },
  "author": "chax.at - Challenge Accepted",
  "license": "ISC",
  "dependencies": {
    "@chax-at/prisma-filter-common": "^2.0.0-alpha.1"
  },
  "devDependencies": {
    "@types/node": "^16.11.26",
    "typescript": "^4.6.2"
  },
  "peerDependencies": {
    "@nestjs/common": "6 - 8"
  },
  "files": [
    "/lib"
  ]
}
